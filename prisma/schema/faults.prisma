model faultText {
  id          String   @id @default(uuid())
  title       String   @unique
    message     String   @db.Text
}


model controlRoomFaults{
  id          String   @id @default(uuid())
  faultID    String
  DMMSDONE String
}